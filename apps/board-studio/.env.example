# Board Studio — Environment Variables
# Copy to .env and adjust as needed

# Kernel API base URL (proxied via Vite in dev)
VITE_API_BASE_URL=http://localhost:8080

# Dev server port
VITE_PORT=3003
