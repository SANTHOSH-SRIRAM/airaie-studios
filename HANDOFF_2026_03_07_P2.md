# AirAIE Audit Fix Implementation — Handoff
> **Date:** 2026-03-07 (Session 3)
> **Context:** Full audit completed, fix tasks created, 0/10 fix tasks started

---

## What Was Done This Session

### Session 2 (Earlier Today)
- Installed deps, fixed 10+ TS build errors, all 3 studios build clean
- Wired `initialRunId`/`initialSessionId` deep-link props
- Added lazy-loading (React.lazy + Suspense) to workflow + agent studios
- Virtualized RunLogs (10K) and RunsListView with @tanstack/react-virtual
- Added ARIA labels across shell + UI components
- Fixed tokenStore tests — all 26 shared tests passing

### Session 3 (This Session)
- Ran 5 parallel audit agents (TypeScript, Security, Dead Code, Tests, Bundle)
- Wrote full audit report to `studios/AUDIT_2026_03_07.md`
- Created 10 implementation tasks (listed below)
- **No fix tasks started yet** — context ran out after audit + task creation

---

## Outstanding Tasks (In Priority Order)

### P0 — Fix Now

**Task #8: Fix XSS in PDF Export**
- File: `apps/board-studio/src/components/boards/ExportDialog.tsx`
- Lines 317-354: `generatePdfHtml()` concatenates user data into HTML without escaping
- Fix: Add `escapeHtml()` utility, apply to `board.name`, `c.name`, `g.name` in template literals
- 10 min effort

**Task #9: Unify Dependency Versions**
- lucide-react: apps have `^0.462.0`, packages have `^0.562.0` → unify to `^0.562.0`
- tailwind-merge: apps have `^2.6.0`, ui has `^3.4.0` → unify to `^3.4.0`
- axios: apps have `^1.7.0`, shared has `^1.13.0` → unify to `^1.13.0`
- Files to edit: `apps/board-studio/package.json`, `apps/workflow-studio/package.json`, `apps/agent-studio/package.json`, possibly `packages/ui/package.json`
- Run `npm install` after, then `npx turbo build` to verify
- 15 min effort

### P1 — This Sprint

**Task #10: Add manualChunks to Vite Configs**
- All 3 `apps/*/vite.config.ts` need `build.rollupOptions.output.manualChunks`:
  - `react-vendor`: react, react-dom, react-router-dom
  - `state`: zustand, @tanstack/react-query
  - `charts`: recharts
  - `ui`: @airaie/ui, @airaie/shell, lucide-react
- 20 min effort

**Task #11: Add URL Validation for window.open Calls**
- Create `packages/shared/src/utils/urlValidation.ts` with `isAllowedOrigin(url, allowedOrigins)` helper
- Apply to 7 locations:
  1. `workflow-studio/src/components/runs/RunArtifactsPanel.tsx`
  2. `workflow-studio/src/components/artifacts/ArtifactCatalog.tsx`
  3. `board-studio/src/pages/WorkflowsPage.tsx`
  4. `board-studio/src/pages/AgentsPage.tsx`
  5. `board-studio/src/components/boards/ExportDialog.tsx`
  6. `packages/shell/src/Header.tsx`
  7. `packages/shell/src/AppShell.tsx`
- 30 min effort

**Task #12: Add ErrorBoundary to 6 Board-Studio Pages**
- File: `apps/board-studio/src/routes.tsx` (or wherever routes are defined)
- Wrap these pages: CardDetailPage, AgentsPage, MemoryPage, WorkflowsPage, ReleasePacketPage, ApprovalsPage
- Import `ErrorBoundary` from `@airaie/ui`
- 20 min effort

**Task #13: Move Duplicate Hooks to @airaie/shared**
- Move `useRuns` from all 3 studios to `packages/shared/src/hooks/useRuns.ts`
- Move `useApprovals` similarly
- Export from `packages/shared/src/index.ts`
- Update imports in all 3 studios
- Run build to verify
- 1 hr effort

**Task #14: Remove Unused Realtime Hook Exports**
- Remove `useRealtimeRun`, `useRealtimeAll` from `packages/shared/src/index.ts`
- Optionally remove `packages/shared/src/realtime/useRealtimeEvents.ts` if nothing else imports it
- 10 min effort

### P2 — Next Sprint

**Task #15: Test API Client Interceptors**
- File to test: `packages/shared/src/api/client.ts`
- Write to: `packages/shared/src/__tests__/client.test.ts`
- Test: token injection, 401 refresh, dedup, retry, APIError transform
- Need `@vitest-environment jsdom` + axios mock
- 2 hr effort

**Task #16: Test Zustand Stores**
- 6 stores to test:
  - `apps/board-studio/src/store/boardStore.ts`
  - `apps/workflow-studio/src/store/canvasStore.ts`
  - `apps/workflow-studio/src/store/uiStore.ts`
  - `apps/agent-studio/src/store/playgroundStore.ts`
  - `apps/agent-studio/src/store/uiStore.ts`
  - `apps/agent-studio/src/store/specStore.ts`
- 1.5 hr effort

**Task #17: Refactor Top as-any Casts with Zod**
- Top targets:
  1. `workflow-studio/src/pages/BuilderPage.tsx` (3 casts)
  2. `board-studio/src/api/plans.ts` (2 casts)
  3. `board-studio/src/pages/BoardsPage.tsx` (3 casts)
  4. `board-studio/src/components/boards/EvidenceSummaryRow.tsx` (3 casts)
  5. `agent-studio/src/pages/BuilderPage.tsx` (4 casts)
- Create Zod schemas for kernel API responses, use `.safeParse()` at API boundary
- 2 hr effort

---

## Current State

- **Build:** All 3 studios build clean (`npx turbo build` passes)
- **Tests:** 26/26 shared package tests passing
- **Audit report:** `studios/AUDIT_2026_03_07.md`
- **Previous handoff:** `studios/HANDOFF_2026_03_07.md` (session 2)

## How to Continue

```bash
cd /Users/santhosh/airaie/studios
cat HANDOFF_2026_03_07_P2.md   # Read this file
# Start with P0 tasks (#8, #9), then P1 (#10-#14), then P2 (#15-#17)
```
